# SOAP - Ejemplo en Python

Este proyecto implementa un servicio SOAP simple usando la librería `spyne`. Permite recibir solicitudes con un nombre y una cantidad de veces, devolviendo un mensaje repetido.

## **Requisitos**

- Python 3.8+
- Librería `spyne` instalada:
  ```bash
  pip install spyne